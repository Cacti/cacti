name: Extended PHP Quality

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-extended-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  COMPOSER_ALLOW_SUPERUSER: 1
  # 10.0 was an unverified placeholder from #6874 (coverage.xml was never actually
  # produced back then, see the two bugs fixed alongside this comment). Measured
  # statement coverage on develop today is ~1.73% (788/45456 via tests/Unit +
  # tests/integration). Set below that measured baseline so the gate catches
  # regressions without failing on day one; raise as real coverage grows.
  COVERAGE_MIN_STATEMENT_PCT: '1.5'

jobs:
#  actionlint:
#    name: actionlint
#    runs-on: ubuntu-latest

#    steps:
#      - name: Checkout
#        uses: actions/checkout@v7.0.0

#      - name: Install actionlint dependencies
#        run: |
#          set -euo pipefail
#          sudo apt-get update
#          sudo apt-get install -y golang-go shellcheck

#      - name: Install actionlint
#        run: |
#          set -euo pipefail
#          GOBIN="${PWD}/.local/bin" go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.7
#          echo "${PWD}/.local/bin" >> "${GITHUB_PATH}"

#      - name: Run actionlint
#        run: |
#          set -euo pipefail
#          "${PWD}/.local/bin/actionlint" -color

#  shell-lint:
#    name: shellcheck + shfmt
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v7.0.0
#
#      - name: Install shell lint dependencies
#        run: |
#          set -euo pipefail
#          sudo apt-get update
#          sudo apt-get install -y shellcheck shfmt
#
#      - name: Run shfmt and shellcheck
#        run: |
#          set -euo pipefail
#          mapfile -t shell_files < <(
#            git ls-files | while read -r file; do
#              [[ -f "${file}" ]] || continue
#              # Exclude vendor and locales
#              case "${file}" in
#                include/vendor/*|locales/*) continue ;;
#                *.sh) echo "${file}"; continue ;;
#              esac
#              # Use portable word boundary for shebang detection
#              if head -n 1 "${file}" | grep -Eq '^#!.*([[:space:]/])(bash|sh)([[:space:]]|$)'; then
#                echo "${file}"
#              fi
#            done | sort -u
#          )
#
#          if [[ "${#shell_files[@]}" -eq 0 ]]; then
#            echo 'No shell files found for linting.'
#            exit 0
#          fi
#
#          # Relax indentation (-i 0) to avoid failing on mixed project styles for now
#          shfmt -d -i 0 -ci "${shell_files[@]}"
#          shellcheck -x "${shell_files[@]}"

  sql-interpolation-ratchet:
    name: sql interpolation ratchet
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Setup PHP 8.4
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2.37.2
        with:
          php-version: '8.4'
          coverage: none

      - name: Block new unparameterised SQL value interpolation
        run: php tests/tools/sql_interpolation_audit.php --check

  validator-constraint-ratchet:
    name: validator constraint ratchet
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Setup PHP 8.4
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2.37.2
        with:
          php-version: '8.4'
          coverage: none

      - name: Block new undocumented no-op input validation
        run: php tests/tools/validator_constraint_audit.php --check

  unit-coverage:
    name: pest coverage gate
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '22'
          cache: npm

      - name: Build managed JavaScript
        run: npm ci

      - name: Setup locked PHP 8.2 dependencies
        uses: ./.github/actions/setup-php-composer
        with:
          php-version: '8.2'
          coverage: xdebug
          extensions: intl, mysql, gd, ldap, gmp, xml, curl, json, mbstring
          ini-values: post_max_size=256M, max_execution_time=60
          dependency-mode: locked-coverage
          install-args: --prefer-dist --no-progress --no-interaction

      - name: Validate composer.json and composer.lock
        run: composer validate --strict

      # Re-enabled: disabled since #6874 (2026-03) pending cleanup of pre-existing
      # issues. Verified the Unit+integration suite passes clean against current
      # develop, fixed the coverage-scope/enforcement-parsing bugs that were
      # silently making this step a no-op, and confirmed real enforcement works.
      - name: Run Pest with coverage
        run: |
          set -euo pipefail
          include/vendor/bin/pest --display-warnings --coverage-clover=coverage.xml \
            --coverage-text --testsuite=Unit,Integration | tee coverage-summary.txt

      - name: Enforce minimum statement coverage
        run: |
          set -euo pipefail
          # shellcheck disable=SC2016
          php -r '
          $min = (float) getenv("COVERAGE_MIN_STATEMENT_PCT");
          if (!file_exists("coverage.xml")) {
              fwrite(STDERR, "coverage.xml was not generated.\n");
              exit(1);
          }
          $xml = @simplexml_load_file("coverage.xml");
          if ($xml === false) {
              fwrite(STDERR, "Failed to parse coverage.xml.\n");
              exit(1);
          }
          $metrics = $xml->project->metrics ?? null;
          if ($metrics === null || !isset($metrics["statements"]) || !isset($metrics["coveredstatements"])) {
              fwrite(STDERR, "Could not determine statement coverage from coverage.xml.\n");
              exit(1);
          }
          $statements = (int) $metrics["statements"];
          $covered = (int) $metrics["coveredstatements"];
          if ($statements <= 0) {
              fwrite(STDERR, "coverage.xml reports zero statements.\n");
              exit(1);
          }
          $pct = ($covered / $statements) * 100.0;
          printf("Statement coverage: %.2f%% (minimum %.2f%%)\n", $pct, $min);
          if ($pct + 1e-9 < $min) {
              fwrite(STDERR, "Coverage gate failed.\n");
              exit(1);
          }
          '

      - name: Upload coverage artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: php-coverage-report
          path: |
            coverage.xml
            coverage-summary.txt
          if-no-files-found: ignore
